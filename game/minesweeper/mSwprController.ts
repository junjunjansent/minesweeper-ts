const resetArray = () => {
  return new Array(5);
};

const main = () => {
  //   const startInput = prompt(
  //     "What level would you like? Type Easy, Med, OR Hard"
  //   )?.toLowerCase;
  // const startInput = prompt("Input guess here OR type 'reset' for game to restart");
};

main();
